
main(){
  const hour = DateTime.now().hour;
  print(hour);
}
/* we can't define the time as a constant variable.
 because it changes in every time we run the code.
 so we define it as an a normal variable (var) that can 
 change constantly */
